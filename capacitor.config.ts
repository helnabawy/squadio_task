import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'squadio_task',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
